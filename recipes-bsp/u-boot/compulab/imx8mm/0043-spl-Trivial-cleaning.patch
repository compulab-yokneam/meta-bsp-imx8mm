From 163bc9f00bd71f8a49a9447aaa15a5ed6d243b93 Mon Sep 17 00:00:00 2001
From: Kirill Kapranov <kirill.kapranov@compulab.co.il>
Date: Tue, 28 Jul 2020 19:22:12 +0300
Subject: [PATCH 43/45] spl: Trivial cleaning

Signed-off-by: Kirill Kapranov <kirill.kapranov@compulab.co.il>
---
 board/compulab/ucm-imx8m-mini/ddr/ddr.c | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/board/compulab/ucm-imx8m-mini/ddr/ddr.c b/board/compulab/ucm-imx8m-mini/ddr/ddr.c
index 7df13c942f..49bce23265 100644
--- a/board/compulab/ucm-imx8m-mini/ddr/ddr.c
+++ b/board/compulab/ucm-imx8m-mini/ddr/ddr.c
@@ -154,7 +154,7 @@ void spl_dram_init(void)
 			while ( 1 ) {};
 		}
 		ddr_info = lpddr4_array[i].id;
-	} else
+	}
 
 	printf("DDRINFO(%s): %s %dG @ %d MHz\n", (ddr_found ? "D" : "?" ), lpddr4_array[i].name,
 			lpddr4_array[i].size, lpddr4_array[i].timing->fsp_table[0]);
@@ -176,8 +176,8 @@ void spl_dram_init(void)
 		do_reset(NULL,0,0,NULL);
 	}
 
-	printf("DDRINFO(M): mr5-8 [ 0x%x ]\n", ddr_info_mrr);
-	printf("DDRINFO(%s): mr5-8 [ 0x%x ]\n", (ddr_found ? "E" : "T" ), ddr_info);
+	printf("DDRINFO(M): mr5-8 [ 0x%.8x ]\n", ddr_info_mrr);
+	printf("DDRINFO(%s): mr5-8 [ 0x%.8x ]\n", (ddr_found ? "E" : "T" ), ddr_info);
 
 	if (ddr_info_mrr != ddr_info) {
 		SPL_TCM_INIT;
-- 
2.11.0

